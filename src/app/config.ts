export const configs = {
    // サイドバーの幅(px)
    sidebarWidth: 348,
    // 元々の.filter-itemのpaddingLeft(px)
    baseIndentWidth: 16,
    // インデントの幅(px)
    indentWidth: 16,
    // ディレクトリ判定基準とするセパレーター文字
    separator: "/",
} as const;
