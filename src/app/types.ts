export type ParseResult = {
    indentDepth: number;
    directoryNames: string[];
    workflowName: string;
};

export type Configs = {
    sidebarWidth: number;
    indentWidth: number;
    separator: string;
};
